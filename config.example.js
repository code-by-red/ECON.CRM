// Arquivo de configuração para desenvolvimento local
// RENOMEIE este arquivo para config.js e configure sua URL

window.ENV = {
  // Substitua pela sua URL do Google Apps Script
  GOOGLE_APPS_SCRIPT_URL: 'https://script.google.com/macros/s/SEU_ID/exec'
};

// Nota: Este arquivo não deve ser commitado no GitHub
// Adicione config.js ao .gitignore
